# Security Simulation

MCPify includes a security package for auditing generated tool surfaces and simulating risky operations before exposing them to agents.

The v1.0 focus is permission metadata and reviewable generated output. Use the generated `permissions.ts` file as the first checkpoint before connecting a server to a real application.

Post-v1 work will expand policy customization, scenario simulation, and security reports.
