# Swagger and OpenAPI

MCPify's v1.0 focus is source analysis. OpenAPI and Swagger support is handled through analyzer packages and is expected to grow after v1.0.

For now, prefer generating from the source services that implement your API behavior.

```bash
mcpify analyze ./src/services --output .mcpify
```
