# Frontend Analysis

MCPify scans frontend code to understand user-facing actions and intent.

## Supported patterns

- React and TSX event handlers.
- Vue component actions.
- Svelte event bindings.
- Angular component and template actions.
- Form submissions and common UI intent patterns.

Use `--no-frontend` when generating a backend-only MCP server.
