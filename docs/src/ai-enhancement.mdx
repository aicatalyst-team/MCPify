# AI Enhancement

AI enhancement is optional in v1.0. When enabled and configured, MCPify can improve generated tool descriptions, workflow summaries, and agent-facing guidance.

The core analyzer and generator work without an AI key. If enhancement is unavailable, MCPify falls back to deterministic descriptions from source metadata.

```bash
mcpify analyze ./app --ai
```
