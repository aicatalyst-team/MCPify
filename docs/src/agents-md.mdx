# agents.md

Every generated server includes an `agents.md` file. It explains the available tools, workflows, permissions, and suggested client configuration.

Use it as the first handoff document for Codex, Claude Desktop, Cursor, or another MCP-capable agent.

The file is generated from the same schema used to build the MCP server, so it should stay aligned with the actual tool surface.
