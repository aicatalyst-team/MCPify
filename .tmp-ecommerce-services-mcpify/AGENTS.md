# AGENTS.md
> Auto-generated by MCPify. This file documents the AI-operable interface for this application.
> Re-run `npx mcpify` to keep it in sync with the codebase.

## Overview

This MCP server exposes **27 tools** and **0 workflows** to AI agents.

| Permission | Count | Meaning |
|---|---|---|
| ✅ SAFE | 16 | Agent may execute autonomously |
| ⚠️ REQUIRES_CONFIRMATION | 11 | Agent must ask user before executing |
| 🚫 BLOCKED | 0 | Human-only — never exposed to AI |
| 🔄 WORKFLOWS | 0 | Multi-step composed operations |

---

## ✅ Safe Tools

These tools are read-only or non-destructive. AI agents may call them without user approval.

- **`getOrderById(orderId: string)`**  
  Retrieves a single order by its ID.

- **`getOrdersByStatus(status: import("C:/Users/amarn/OneDrive/Desktop/kochi/MCPify/examples/ecommerce-saas/src/services/orders").OrderStatus, limit: number, offset: number)`**  
  Lists all orders with an optional status filter and pagination.

- **`countOrdersByStatus()`**  
  Returns the total count of orders grouped by status.

- **`searchOrders(query: string)`**  
  Searches orders by customer email or name.

- **`updateOrderStatus(orderId: string, status: import("C:/Users/amarn/OneDrive/Desktop/kochi/MCPify/examples/ecommerce-saas/src/services/orders").OrderStatus)`**  
  Updates the fulfillment status of an order.

- **`listTicketsByStatus(status: import("C:/Users/amarn/OneDrive/Desktop/kochi/MCPify/examples/ecommerce-saas/src/services/support").TicketStatus)`**  
  List support tickets filtered by status.

- **`getTicketById(ticketId: string)`**  
  Get a single support ticket by ID.

- **`searchTickets(query: string)`**  
  Search tickets by keyword in subject or body.

- **`resolveTicket(ticketId: string, resolution: string)`**  
  Marks a support ticket as resolved.

- **`getUserByEmail(email: string)`**  
  Find a user by their email address.

- **`getUserById(userId: string)`**  
  Get a user by ID.

- **`listUsers(role: "customer" | "admin" | "agent" | undefined)`**  
  List all users with optional role filter.

- **`searchUsers(query: string)`**  
  Search users by name or email fragment.

- **`registerUser(email: string, name: string, password: string)`**  
  Registers a new user account.

- **`authenticateUser(email: string, password: string)`**  
  Authenticates a user and returns a session token.

- **`updateUserRole(userId: string, newRole: "customer" | "admin" | "agent")`**  
  Updates a user's role (admin action).

---

## ⚠️ Tools Requiring Confirmation

These tools perform mutating or consequential operations. The AI agent **must** ask the user
for explicit approval and then call the tool again with `{ "__confirmed": true }`.

- **`refundOrder(orderId: string, amount: number | undefined)`**  
  Issues a full or partial refund for an order.

- **`cancelOrder(orderId: string, reason: string)`**  
  Cancels a pending or processing order.

- **`assignOrderToAgent(orderId: string, agentId: string)`**  
  Assigns an order to a fulfilment agent.

- **`createSupportRequest(customerId: string, subject: string, body: string, orderId: string | undefined)`**  
  Creates a new support ticket.

- **`replyToTicket(ticketId: string, message: string)`**  
  Sends a reply message to a support ticket.

- **`assignTicket(ticketId: string, agentId: string)`**  
  Assigns a ticket to a support agent.

- **`escalateTicket(ticketId: string, reason: string)`**  
  Escalates a ticket to a higher-tier agent.

- **`sendMessageToCustomer(customerId: string, message: string)`**  
  Sends a direct message to a customer.

- **`suspendUser(userId: string, reason: string)`**  
  Suspends a user account.

- **`initiatePasswordReset(email: string)`**  
  Sends a password reset email.

- **`inviteTeamMember(email: string, role: "customer" | "admin" | "agent")`**  
  Invites a new team member by email.

---

## 🚫 Blocked Tools

These tools are **never** exposed to AI agents. They can only be run manually by a human.

_None_

---

## 🔄 Workflows

_No workflows detected._

---

## Connecting an AI Agent

### Claude Desktop

Add to `~/Library/Application Support/Claude/claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "my-app": {
      "command": "node",
      "args": ["C:\Users\amarn\OneDrive\Desktop\kochi\MCPify\.tmp-ecommerce-services-mcpify\dist\.tmp-ecommerce-services-mcpify\server.js"]
    }
  }
}

```

### Cursor

Add to `.cursor/mcp.json` in your project root:

```json
{
  "mcpServers": {
    "my-app": {
      "command": "node",
      "args": ["C:\Users\amarn\OneDrive\Desktop\kochi\MCPify\.tmp-ecommerce-services-mcpify\dist\.tmp-ecommerce-services-mcpify\server.js"]
    }
  }
}
```

### Example Interaction

```
User: Refund order #12345 and notify the customer.

Agent: This will execute refundOrder (REQUIRES_CONFIRMATION).
       Shall I proceed?

User:  Yes.

Agent: ✓ refundOrder(orderId="12345") — success
       ✓ sendMessage(customerId="cust_99") — notification sent
```

---

*Generated by MCPify · 2026-06-06T20:02:18.492Z*
